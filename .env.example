# Required in non-local deployments; use a unique secret of at least 32 characters.
API_RATE_LIMIT_HMAC_KEY=change-me-to-a-32-plus-character-secret
# Uvicorn listen port.
PORT=8080
# Uvicorn listen address.
HOST=0.0.0.0
